<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MyActivity">

    <EditText
        android:id="@+id/etSomeText"
        android:layout_centerHorizontal="true"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginTop="20dp"
        android:inputType="text"
        android:hint="Type some text here."/>
    <Button
        android:id="@+id/btClickHere"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Click here to show text"
        android:layout_below="@+id/etSomeText"
        android:layout_marginTop="20dp"
        android:singleLine="false"
        android:onClick="Button1_Click"/>
    <TextView
        android:id="@+id/tvSomeText"
        android:layout_centerInParent="true"
        android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btClickHere"
        android:layout_marginTop="20dp" />
</RelativeLayout>
